RewriteEngine On
RewriteBase /PROYECTO/backend/public/

# Si el archivo o carpeta no existe, redirige a index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
